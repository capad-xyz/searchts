# Groq (optional transcribe extra)

`searchts transcribe` is subtitles-first via yt-dlp. Groq Whisper is an **optional** extra if a Groq key is set. It is not required for subtitles.

Do not run `searchts install --env=auto` for this.

Install the package: [docs/install.md](../../docs/install.md)
