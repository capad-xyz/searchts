"""Reproducible unlocker benchmark for searchts.

Run:  python -m benchmarks.run

Measures how often searchts reads a set of (often bot-walled) pages, which tier
carried each read, and how long it took — a proof point, a regression canary,
and an easy first contribution (add a case). See benchmarks/README.md.
"""
